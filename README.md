# ShinyProgress
A simple progress tracker built entirely using Python. The tool relies on the Shiny package for the web interface, while everything else is done with generic packages like matplotlib and numpy.
